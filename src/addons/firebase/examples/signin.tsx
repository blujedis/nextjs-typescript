import Layout from 'layout';
import FirebaseAuth from 'addons/firebase/components/firebaseui';

const Auth = () => {
  return (
    <Layout>
      <FirebaseAuth />
    </Layout>

  );
}

export default Auth